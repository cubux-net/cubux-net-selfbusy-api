Тип `Cubux.MaterialSource`
==========================

Материал, как таковой. Неисчисляемое сырьё.

Объект со следующими полями:

Поле | Тип | Описание
---- | --- | --------
**`uuid`** PK | `uuid`                   | UUID
`name` | `string` | Название. Максимум 128 символов
`is_hidden` | `boolean` | Является ли скрытым
